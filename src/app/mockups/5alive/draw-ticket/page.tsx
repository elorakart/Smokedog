"use client";

import { FiveAliveTableMockup } from "@/components/mockups/five-alive/FiveAliveTableMockup";

export default function FiveAliveDrawTicketPage() {
  return (
    <FiveAliveTableMockup
      title="Draw & Play — Vintage Ticket"
      description="Draw pile interaction with vintage ticket-stub card faces: perforated edges, serial numbers, and stamp-style category labels."
      variant="ticket"
      variantLabel="Vintage ticket"
    />
  );
}
