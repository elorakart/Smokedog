"use client";

import { FiveAliveTableMockup } from "@/components/mockups/five-alive/FiveAliveTableMockup";

export default function FiveAliveDrawPulsePage() {
  return (
    <FiveAliveTableMockup
      title="Draw & Play — Pulse Badge"
      description="Same draw/play flow with pulse-style cards: circular value badges, category color chips, and soft glow accents. Draw pile count stays pinned on the stack."
      variant="pulse"
      variantLabel="Pulse badge"
    />
  );
}
